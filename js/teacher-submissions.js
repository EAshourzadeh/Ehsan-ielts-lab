document.addEventListener("DOMContentLoaded",()=>requireAdminAuth(async()=>{document.getElementById("btnLogout").onclick=logoutAdmin;const results=await getResults();document.getElementById("submissionRows").innerHTML=Object.values(results).sort((a,b)=>String(b.submittedAt).localeCompare(String(a.submittedAt))).map(r=>`<tr><td>${escapeHtml(r.studentName)}</td><td>${escapeHtml(r.examName||r.examId)}</td><td>${r.submittedAt?new Date(r.submittedAt).toLocaleString():"—"}</td><td>${r.listeningBand??"—"}</td><td>${r.readingBand??"—"}</td><td>${escapeHtml(r.status||"pending")}</td><td><a class="btn btn-sm" href="teacher-grading.html?result=${encodeURIComponent(r.id)}">Grade</a></td></tr>`).join("")||'<tr><td colspan="7" class="muted">No submissions yet.</td></tr>'; }));
